import std.*;

public record message{
    char messageType;
}

public record standardMessage extends message{
    int val; 
    char[] dirArr;
}

// "Record" is not recognized symbol (page 152 of book)
//public record channelMessage extends message{
//    int w_dest; 
//    int h_dest; 
//    chan<Record>.write out;
//}

// channelMessage cannot contain a channel of the same record type
public record channelMessage1{
    int w_dest; 
    int h_dest; 
    chan<channelMessage1>.write out;
}

// channelMessage cannot contain a channel of the super class

public record channelMessage2 extends message{
    int w_dest; 
    int h_dest; 
    chan<message>.write out;
}

public void main(string[] args){
    chan<message> input; 
    chan<message> output;

    
}
